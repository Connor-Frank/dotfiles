#!/bin/sh

while true; do
  wifistatus="$(cat /sys/class/net/w*/operstate 2>/dev/null)"
  if [ "$wifistatus" = "up" ]; then
    echo $(speedtest --simple) > ~/.local/share/speedtest-result
  fi
  sleep 10m
done
